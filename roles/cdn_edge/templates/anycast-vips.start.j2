#!/bin/sh
# Bind anycast VIPs to lo so nginx can listen on them. Idempotent.
{% for vip in cdn_edge_anycast_v4 %}
ip -4 addr add {{ vip }} dev lo 2>/dev/null || true
{% endfor %}
{% for vip in cdn_edge_anycast_v6 %}
ip -6 addr add {{ vip }} dev lo 2>/dev/null || true
{% endfor %}
exit 0
